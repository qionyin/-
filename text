simple_rag/
│
├── 📄 __init__.py              # 包初始化
├── ⚙️  config.py               # 配置管理中心
├── 🔢  embedding_utils.py      # 向量嵌入与检索
├── 🔍  retriever.py            # 混合检索器
├── 💬  llm_client.py           # 通义千问客户端
├── 🎯  rag_system.py           # RAG主系统（编排层）
├── 🛠️  utils.py                # 工具函数
├── 🚀  main.py                 # 使用示例
├── 📦  requirements.txt        # Python依赖
├── 🔑  .env.example            # 环境变量模板
└── 📖  README.md               # 项目文档


RAGConfig
├── 通义千问配置
│   ├── dashscope_api_key
│   └── qwen_model_name
├── 数据库配置
│   ├── chroma_persist_dir
│   └── collection_name
├── 嵌入模型配置
│   └── embedding_model_path
└── 检索参数
    ├── top_k
    ├── chunk_size
    └── max_context_chars


HybridRetriever (混合检索器)
├── EmbeddingUtils (向量检索)
│   ├── SentenceTransformer (BGE-M3)
│   ├── embed_documents()
│   ├── embed_query()
│   └── similarity_search() → ChromaDB
└── BM25Indexer (关键词检索)
    ├── tokenize()
    ├── build()
    └── search()


用户查询
  ↓
向量编码 (Embedding)
  ↓
ChromaDB相似性搜索 → 向量结果
  ↓
BM25关键词搜索 → 关键词结果
  ↓
结果融合与排序
  ↓
Top-K相关文档


QwenLLMClient
├── Tongyi (LangChain封装)
├── chat() - 单次对话
│   ├── SystemMessage (系统提示)
│   ├── HumanMessage (用户问题+上下文)
│   └── StrOutputParser (输出解析)
└── chat_with_history() - 多轮对话
    └── 消息历史管理


检索到的文档
  ↓
上下文格式化
  ↓
构建Prompt (系统提示 + 用户问题 + 上下文)
  ↓
LangChain调用
  ↓
DashScope API (通义千问3)
  ↓
生成答案


Utility Functions
├── normalize_whitespace() - 文本标准化
├── format_context() - 上下文格式化
│   └── [1] 来源#ID: 内容片段
└── extract_citations() - 引用提取
    └── [{id, source, chunk_id}]


┌─────────────┐
│  文档入库阶段  │
└─────────────┘
     ↓
1. PDF/文本 → 文本提取
2. 文本分块 (Chunking)
3. 生成嵌入向量 (Embedding)
4. 存储到ChromaDB
5. 构建BM25索引

┌─────────────┐
│  查询回答阶段  │
└─────────────┘
     ↓
1. 接收用户问题
2. 向量检索 (语义匹配)
3. BM25检索 (关键词匹配)
4. 结果融合与重排序
5. 提取Top-K文档
6. 格式化上下文
7. 构建Prompt
8. 调用通义千问
9. 生成答案
10. 返回答案+引用




实现混合检索策略：
- **向量检索**: 使用BGE-M3模型进行语义匹配
- **BM25检索**: 基于关键词的传统检索
- **结果融合**: 综合两种检索结果并重排序

### LLM客户端 (llm_client.py)

封装通义千问API调用：
- 单次对话: `chat()`
- 多轮对话: `chat_with_history()`
- 上下文管理: 自动构建prompt

### 记忆系统 (cst/memory_manager.py)

#### 短期记忆
- 存储当前会话的对话历史
- 限制最大轮数避免上下文过长
- 支持获取最近N条消息

#### 长期记忆
- 基于ChromaDB的持久化存储
- 支持向量搜索和关键词搜索
- 自动保存仿真结果和优化历史
- 标签分类管理

### CST接口 (cst/cst_tools.py)

提供CST软件交互功能：
- 加载CST项目文件
- 读取仿真参数
- 解析结果文件(S参数、VSWR、增益等)
- 生成一句话结果总结

### CST优化器 (cst/cst_rag.py)

实现完整的优化循环：
1. **结果分析**: 读取当前仿真结果
2. **历史检索**: 搜索相似的历史仿真记录
3. **建议生成**: LLM分析并生成优化建议
4. **参数修改**: 应用优化建议到CST模型
5. **记忆保存**: 自动保存仿真结果到长期记忆

## ⚙️ 配置文件

### 环境变量配置

| 变量名 | 说明 | 示例 |
|--------|------|------|
| DASHSCOPE_API_KEY | 通义千问API密钥 | sk-xxxxxxxxx |
| CST_PROJECT_PATH | CST项目默认路径 | D:\Projects\antenna.cst |
| CST_RESULTS_PATH | CST结果默认路径 | D:\Results\simulation_001 |

### 快速模式 vs 向量模式

系统支持两种运行模式：

**快速模式** (`use_fast_mode=True`)
- ✅ 速度快，不使用向量嵌入
- ✅ 适合快速测试和演示
- ❌ 仅支持关键词搜索

**向量模式** (`use_fast_mode=False`)
- ✅ 支持语义搜索，更准确
- ✅ 适合生产环境
- ❌ 速度较慢，需要生成嵌入向量
